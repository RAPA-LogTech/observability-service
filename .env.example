# Service metadata
SERVICE_NAME=observability-service
ENVIRONMENT=local
DATA_SOURCE_MODE=real_only

# Local CORS settings (comma-separated)
ALLOWED_ORIGINS=http://localhost:3000,http://127.0.0.1:3000

# Real AWS backends
OPENSEARCH_URL=https://vpc-log-platform-dev-berciu3s6yeeq6getvwavbebz4.ap-northeast-2.es.amazonaws.com
OPENSEARCH_LOGS_INDEX=logs-*
OPENSEARCH_TRACES_INDEX=traces-*
OPENSEARCH_TIMEOUT_SECONDS=8
OPENSEARCH_VERIFY_TLS=true
# For SSH tunnel testing (OPENSEARCH_URL=https://localhost:9200), set false.
# OPENSEARCH_VERIFY_TLS=false
OPENSEARCH_USERNAME=
OPENSEARCH_PASSWORD=
# OPENSEARCH_API_KEY=
AMP_ENDPOINT=https://aps-workspaces.ap-northeast-2.amazonaws.com/workspaces/ws-438cd95b-8a3a-4106-b8e9-9b7c8af4f5f8/api/v1/remote_write
AMP_TIMEOUT_SECONDS=8
AMP_STEP_SECONDS=60
# AMP_DEFAULT_SERVICE=checkout
# AMP_ERROR_RATE_QUERY=(sum(rate(http_requests_total{service="$service",status=~"5.."}[5m])) / clamp_min(sum(rate(http_requests_total{service="$service"}[5m])), 1)) * 100
# AMP_LATENCY_P95_QUERY=histogram_quantile(0.95, sum(rate(http_request_duration_seconds_bucket{service="$service"}[5m])) by (le)) * 1000
# AMP_THROUGHPUT_QUERY=sum(rate(http_requests_total{service="$service"}[1m]))
