# Service metadata
SERVICE_NAME=observability-service
ENVIRONMENT=local

# Local CORS settings (comma-separated)
ALLOWED_ORIGINS=http://localhost:3000,http://127.0.0.1:3000

# Optional: connect real AWS backends
# OPENSEARCH_URL=https://your-opensearch-endpoint
# AMP_ENDPOINT=https://aps-workspaces.<region>.amazonaws.com/workspaces/<workspace-id>
